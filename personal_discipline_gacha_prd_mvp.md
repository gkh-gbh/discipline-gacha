# 个人自律抽卡系统：轻量 PRD + MVP 功能清单 + 数据结构草案

## 1. 项目概述

### 1.1 项目名称
暂定名：**现实任务抽卡系统**

可选命名方向：

- 人生任务系统
- Reality Quest
- Quest Wallet
- 周末卡池
- 自律召唤
- 生活主线

### 1.2 项目定位
这是一个主要服务个人使用的轻量化网页应用，用游戏化任务、延迟满足、抽卡仪式感和奖励预算机制，帮助用户更稳定地完成现实生活中的长期目标。

它不是传统待办清单，也不是完整游戏，而是一个将现实任务转化为“任务完成 → 获得抽卡资源 → 周末抽卡 → 解锁快乐预算”的个人成长系统。

### 1.3 核心目标

让用户在完成现实任务后，获得明确、可期待、低心理负担的正反馈，从而提升持续行动能力。

### 1.4 核心原则

1. **先服务自己，不追求商业化。**
2. **先验证机制，不追求复杂美术。**
3. **奖励来自预设预算，不制造额外消费压力。**
4. **任务必须降低启动成本，而不是增加负担。**
5. **抽卡提供期待感，但不能让努力完全看运气。**
6. **系统应该帮助用户行动，而不是惩罚用户失败。**

---

## 2. 用户与使用场景

### 2.1 目标用户
第一阶段只考虑单一用户：开发者本人。

用户特征：

- 有一些想长期坚持但经常失败的习惯。
- 有现实中的课程、学习、工作或生活任务需要推进。
- 对游戏化、抽卡、稀有度、奖励机制有兴趣。
- 希望完成任务后能更无负担地使用娱乐预算。
- 希望系统提供仪式感，而不是普通待办清单。

### 2.2 核心使用场景

#### 场景 A：每天上线完成基础任务
用户打开网页，看到当天自动生成的每日任务。每日任务很简单，主要用于启动状态和提供正反馈。

示例：

- 喝一杯水
- 整理桌面 2 分钟
- 打开学习资料
- 写一句今日记录
- 出门走 5 分钟

#### 场景 B：推进长期习惯任务
用户设置系列任务，比如早睡、跑步、读书、背单词。系统记录连续完成情况，并给予更高奖励。

示例：

- 本周跑步 3 次
- 连续 5 天 0 点前睡觉
- 每天阅读 20 分钟
- 每天背 30 个单词

#### 场景 C：推进现实主线任务
用户将课程要求、项目任务、考试准备等放入主线任务。完成主线任务可以获得较多抽卡资源。

示例：

- 完成课程作业第一版
- 复习第 3 章
- 写完论文大纲
- 提交项目报告

#### 场景 D：周末抽卡领取奖励
用户平日完成任务积累抽卡资源，周末进入卡池页面抽卡，获得不同稀有度奖励。奖励金额进入“快乐预算”，可以毫无心理负担地使用。

---

## 3. 产品核心循环

### 3.1 主循环

```text
查看今日任务
  ↓
完成任务
  ↓
获得抽卡资源 / 稳定碎片
  ↓
积累到周末
  ↓
开放卡池
  ↓
抽卡获得奖励
  ↓
奖励进入快乐预算
  ↓
使用奖励并记录
  ↓
继续下一周
```

### 3.2 设计意图

这个循环的关键不是即时返钱，而是通过“周末开放卡池”制造持续期待感。

平日完成任务时，用户获得的是一种“未来奖励的可能性”；周末抽卡时，用户获得仪式化反馈；奖励预算则让用户对娱乐消费更少内疚。

---

## 4. MVP 范围

### 4.1 MVP 目标

MVP 的目标不是做出完整游戏体验，而是验证以下问题：

1. 用户是否愿意每天打开系统？
2. 每日任务是否能帮助用户进入行动状态？
3. 系列任务和主线任务是否能提高现实任务完成率？
4. 周末抽卡是否能产生期待感？
5. 奖励预算是否能让用户更无负担地消费？
6. 任务、抽卡、奖励之间的数值是否平衡？

### 4.2 MVP 不做的内容

第一版暂不做：

- 多用户系统
- 社交功能
- 排行榜
- 商店系统
- 复杂角色养成
- 大量卡牌插画
- 3D 动画
- 原生 App
- AI 自动生成任务
- 复杂权限系统
- 云端同步的强依赖

### 4.3 MVP 必须包含的功能

MVP 应包含 5 个核心模块：

1. 任务模块
2. 资源模块
3. 卡池模块
4. 奖励钱包模块
5. 数据统计模块

---

## 5. 功能清单

## 5.0 任务创建原则：极简优先

### 核心判断

任务创建是整个系统最容易流失用户的环节。如果用户在添加任务时需要填写太多字段，例如描述、截止日期、分类、奖励、周期、标签等，就会产生额外心理负担，反而降低使用意愿。

因此，MVP 阶段的任务创建必须遵循一个原则：

> 用户只需要输入任务名字和难度，就应该能创建一个可用任务。

### MVP 创建方式

默认创建任务时只需要两个字段：

1. **任务名字**
2. **任务难度**

例如：

```text
任务名字：完成数学作业
难度：中等
```

系统根据难度自动推断：

- 任务类型默认值
- 宝石奖励
- 星尘奖励
- 是否属于今日任务
- 是否需要强调显示

### 难度与奖励映射

| 难度 | 使用场景 | 宝石奖励 | 星尘奖励 |
|---|---|---:|---:|
| 简单 | 几分钟能完成的小任务 | 10 | 0 |
| 普通 | 需要一定行动成本的任务 | 30 | 1 |
| 中等 | 需要认真完成的任务 | 60 | 3 |
| 困难 | 明显消耗精力的重要任务 | 100 | 6 |
| 突破 | 拖延很久或心理阻力很大的任务 | 150 | 10 |

### 默认任务类型策略

为了进一步降低输入成本，第一版可以不强制用户选择任务类型，而是采用默认策略：

- 从首页快速添加的任务，默认视为今日任务或临时任务。
- 从系列任务页面添加的任务，默认视为系列任务。
- 从主线任务页面添加的任务，默认视为主线任务。

也就是说，任务类型由入口决定，而不是让用户每次都手动选择。

### 可选高级设置

高级字段不应该出现在默认创建流程中，而应该放进“展开更多设置”里。

可选字段包括：

- 任务描述
- 截止日期
- 重复周期
- 所属分类
- 自定义奖励
- 阶段目标
- 备注

默认情况下，用户完全不需要填写这些字段。

### 推荐创建流程

```text
点击添加任务
  ↓
输入任务名字
  ↓
选择难度
  ↓
点击创建
  ↓
系统自动生成奖励和任务配置
```

### 推荐交互形式

为了让创建任务更轻，可以设计成首页底部的快速输入框：

```text
[ 输入一个任务... ] [难度：普通 ▼] [添加]
```

也可以支持自然语言快捷输入：

```text
完成英语作文 #中等
跑步 20 分钟 #简单
写完报告初稿 #困难
```

MVP 阶段不必立刻支持复杂自然语言解析，但可以先预留设计空间。

## 5.1 首页 / 今日面板

### 功能说明
用户每天打开网页后首先看到的页面，用于快速了解今天该做什么、当前资源状态、距离周末卡池还有多久。

### MVP 功能

- 显示今日日期
- 显示今日每日任务
- 显示进行中的系列任务
- 显示进行中的主线任务
- 显示今日已获得资源
- 显示本周累计抽卡资源
- 显示距离周末卡池开放的倒计时
- 显示当前快乐预算余额

### 推荐信息结构

```text
今日状态
- 今天完成任务数：3 / 6
- 今日获得抽卡资源：40
- 本周累计抽卡资源：180
- 当前快乐预算：¥85
- 距离卡池开放：2 天

今日任务
- 每日任务
- 系列任务
- 主线任务
```

---

## 5.2 每日任务模块

### 功能说明
每日任务是系统最低门槛的任务类型，主要作用是引导用户上线、建立信心、提供稳定正反馈。

### 设计原则

- 难度必须低。
- 数量不宜过多。
- 完成后奖励较少但稳定。
- 不应该让用户感到压力。
- 可以自动每日刷新。

### MVP 功能

- 创建每日任务模板
- 每天根据模板生成今日任务
- 标记任务完成
- 完成后发放少量资源
- 显示今日完成状态

### 示例任务

| 任务 | 奖励 |
|---|---:|
| 喝一杯水 | 5 宝石 |
| 整理桌面 2 分钟 | 5 宝石 |
| 打开学习材料 | 10 宝石 |
| 写一句今日记录 | 10 宝石 |

### MVP 规则

- 每日任务每天自动刷新。
- 每日任务当天未完成则过期。
- 每个每日任务只能完成一次。
- 每日任务奖励低，不影响整体平衡。

---

## 5.3 系列任务模块

### 功能说明
系列任务用于承载用户想长期坚持但经常失败的行为，比如早睡、跑步、阅读、背单词等。

### 设计原则

- 关注连续性和阶段进度。
- 允许失败，但不应强惩罚。
- 应该给予比每日任务更明显的奖励。
- 可以有阶段目标和连续奖励。

### MVP 功能

- 创建系列任务
- 设置任务名称
- 设置任务描述
- 设置周期目标
- 设置每次完成奖励
- 设置阶段完成奖励
- 记录完成次数
- 显示进度条

### 示例任务

| 系列任务 | 周期目标 | 每次奖励 | 阶段奖励 |
|---|---:|---:|---:|
| 跑步 | 每周 3 次 | 20 宝石 | 完成本周目标额外 50 宝石 |
| 早睡 | 每周 5 次 | 15 宝石 | 完成本周目标额外 50 宝石 |
| 阅读 | 每周 5 次 | 15 宝石 | 完成本周目标额外 40 宝石 |

### MVP 规则

- 系列任务可以按周统计。
- 每天同一系列任务最多完成一次。
- 达成本周目标后发放阶段奖励。
- 失败不扣资源，只是不获得额外奖励。

---

## 5.4 主线任务模块

### 功能说明
主线任务用于承载现实中更重要、更具体的目标，比如课程、考试、项目、论文、工作任务等。

### 设计原则

- 主线任务应该和现实责任绑定。
- 奖励应该明显高于每日任务。
- 任务可以拆成多个阶段。
- 完成主线任务应该带来强反馈。

### MVP 功能

- 创建主线任务
- 设置标题、描述、截止日期
- 设置任务难度
- 设置奖励资源
- 标记完成
- 显示任务状态

### 任务难度建议

| 难度 | 示例 | 奖励建议 |
|---|---|---:|
| 简单 | 复习一节课、整理资料 | 20 宝石 |
| 中等 | 完成一次作业、写一页报告 | 50 宝石 |
| 困难 | 完成论文大纲、提交项目 | 100 宝石 |
| 突破 | 解决长期拖延的大任务 | 150 宝石 + 稀有券 |

### MVP 规则

- 主线任务不会自动刷新。
- 用户手动创建和完成。
- 主线任务可以设置截止日期。
- 完成后立即获得资源。
- 逾期暂不惩罚，只显示逾期状态。

---

## 5.5 资源系统

### 功能说明
资源系统连接任务和抽卡。用户完成任务后不直接获得金钱，而是获得抽卡资源。

### MVP 资源类型

建议第一版使用两种资源：

1. **宝石**：用于周末抽卡。
2. **星尘**：用于稳定兑换或保底进度。

### 设计目的

- 宝石提供随机奖励和期待感。
- 星尘提供确定性，避免努力完全看运气。

### MVP 规则

- 完成任务获得宝石。
- 完成较重要任务额外获得星尘。
- 每 100 宝石可以抽 1 次卡。
- 星尘可以用于兑换固定奖励，或作为保底进度。

### 示例奖励

| 任务类型 | 宝石奖励 | 星尘奖励 |
|---|---:|---:|
| 每日任务 | 5–10 | 0 |
| 系列任务 | 15–30 | 1–2 |
| 系列阶段完成 | 40–80 | 5 |
| 主线任务 | 30–150 | 3–10 |
| 突破任务 | 150–300 | 10–20 |

---

## 5.6 卡池系统

### 功能说明
卡池是系统的仪式感核心。用户平日积累宝石，周末进入卡池页面抽取奖励。

### MVP 功能

- 周末开放卡池
- 平日显示倒计时
- 用户消耗宝石抽卡
- 显示抽卡动画
- 显示抽卡结果
- 记录抽卡历史
- 根据稀有度发放奖励金额

### 卡池开放规则

第一版建议：

- 每周六 00:00 开放。
- 每周日 23:59 关闭。
- 平日不能抽卡，只能查看资源和倒计时。

### 抽卡消耗

```text
单抽：100 宝石
十连：900 宝石
```

第一版可以只做单抽，避免复杂性。

### 稀有度设计

| 稀有度 | 概率 | 奖励金额 |
|---|---:|---:|
| N | 60% | ¥5 |
| R | 25% | ¥15 |
| SR | 10% | ¥30 |
| SSR | 4% | ¥80 |
| UR | 1% | ¥200 |

### 保底规则建议

MVP 版可以设置简单保底：

- 每 10 抽至少 1 个 SR 或以上。
- 每 50 抽至少 1 个 SSR 或以上。
- 未触发高稀有奖励时，保底计数累积。

如果第一版嫌复杂，可以只做：

- 每 10 抽至少一次 SR。

### 重要限制

每月奖励总额不能无限制增加。建议设置月度奖励预算。

例如：

```text
每月快乐预算池：¥500
抽卡奖励从该预算池中解锁
预算池耗尽后，抽卡只获得象征性奖励或下月生效奖励
```

---

## 5.7 奖励钱包模块

### 功能说明
奖励钱包用于记录用户通过抽卡解锁的可自由使用金额。

### 设计目的

让用户可以对娱乐消费更少内疚，因为这笔钱是通过完成任务从预设预算中解锁出来的。

### MVP 功能

- 显示当前快乐预算余额
- 显示累计获得奖励
- 记录每次抽卡获得的金额
- 记录奖励消费
- 手动新增消费记录
- 显示剩余可用金额

### 示例消费记录

| 日期 | 用途 | 金额 |
|---|---|---:|
| 2026-05-04 | 买咖啡 | ¥28 |
| 2026-05-05 | 买游戏 | ¥68 |
| 2026-05-08 | 吃甜点 | ¥35 |

### MVP 规则

- 抽卡奖励进入钱包余额。
- 用户可以手动记录使用金额。
- 钱包余额不能低于 0。
- 每月可设置奖励预算上限。

---

## 5.8 数据统计模块

### 功能说明
数据统计用于让用户看到自己确实在进步。

### MVP 功能

- 今日完成任务数
- 本周完成任务数
- 本周获得宝石数
- 本周抽卡次数
- 当前连续使用天数
- 系列任务完成进度
- 主线任务完成数量
- 累计解锁奖励金额

### 后续可扩展统计

- 每周完成率趋势
- 哪类任务完成最多
- 哪类任务最容易失败
- 奖励金额与完成率关系
- 月度总结报告

---

## 6. 页面结构草案

### 6.1 MVP 页面列表

第一版建议只做 5 个页面：

1. 首页 / 今日面板
2. 任务管理页
3. 卡池页
4. 奖励钱包页
5. 设置页

---

## 6.2 首页 / 今日面板

### 页面目的
快速进入今日任务状态。

### 页面内容

- 今日日期
- 今日完成进度
- 每日任务列表
- 系列任务列表
- 主线任务列表
- 本周宝石数量
- 当前星尘数量
- 快乐预算余额
- 周末卡池倒计时

---

## 6.3 任务管理页

### 页面目的
创建、编辑、管理任务。

### 页面内容

- 每日任务模板管理
- 系列任务管理
- 主线任务管理
- 新建任务按钮
- 任务状态筛选

---

## 6.4 卡池页

### 页面目的
提供抽卡仪式感。

### 页面内容

- 当前卡池状态：开放 / 未开放
- 倒计时
- 当前宝石数量
- 抽卡按钮
- 卡池概率说明
- 抽卡动画区域
- 抽卡结果展示
- 抽卡历史
- 保底进度

---

## 6.5 奖励钱包页

### 页面目的
管理已解锁奖励和消费记录。

### 页面内容

- 当前可用快乐预算
- 本月预算上限
- 本月已解锁金额
- 本月已消费金额
- 消费记录列表
- 新增消费记录按钮

---

## 6.6 设置页

### 页面目的
配置系统基础规则。

### 页面内容

- 每月奖励预算上限
- 每次抽卡消耗宝石
- 卡池开放时间
- 稀有度概率
- 稀有度奖励金额
- 是否启用保底
- 数据导出 / 清空

---

## 7. 关键用户流程

### 7.1 每日任务完成流程

```text
打开首页
  ↓
查看今日每日任务
  ↓
点击完成
  ↓
系统发放宝石
  ↓
任务状态变为已完成
  ↓
首页资源数量更新
```

### 7.2 系列任务完成流程

```text
打开首页
  ↓
查看系列任务
  ↓
完成一次任务
  ↓
系统记录本周进度
  ↓
发放宝石 / 星尘
  ↓
如果达到阶段目标，发放额外奖励
```

### 7.3 主线任务完成流程

```text
打开任务管理页或首页
  ↓
查看主线任务
  ↓
完成现实任务
  ↓
点击完成
  ↓
系统发放较高资源
  ↓
主线任务进入已完成状态
```

### 7.4 周末抽卡流程

```text
进入卡池页
  ↓
检查是否为开放时间
  ↓
检查宝石是否足够
  ↓
点击抽卡
  ↓
播放抽卡动画
  ↓
计算稀有度和奖励
  ↓
显示结果
  ↓
奖励金额进入钱包
  ↓
记录抽卡历史
```

### 7.5 奖励消费流程

```text
进入奖励钱包页
  ↓
查看可用快乐预算
  ↓
进行现实消费
  ↓
手动记录消费用途和金额
  ↓
钱包余额减少
  ↓
保留消费记录
```

---

## 8. 数值设计草案

## 8.1 资源命名

第一版建议使用简单命名：

- 宝石：抽卡资源
- 星尘：稳定兑换 / 保底资源
- 快乐预算：可自由使用的钱

## 8.2 每周资源预期

假设一周完成：

- 每日任务 4 个 / 天，每个平均 7 宝石
- 系列任务 5 次，每次 20 宝石
- 主线任务 3 个，每个平均 60 宝石

则一周获得：

```text
每日任务：4 × 7 × 7 = 196 宝石
系列任务：5 × 20 = 100 宝石
主线任务：3 × 60 = 180 宝石
合计：476 宝石
```

如果 100 宝石抽 1 次，一周约 4–5 抽。

这个节奏比较合适：

- 不会太快通胀。
- 每周能抽几次，有期待感。
- 主线任务对抽卡次数影响明显。

## 8.3 奖励预算建议

建议设置月度奖励预算，而不是让抽卡无限返钱。

示例：

```text
每月奖励预算：¥500
每周理论解锁：¥100–150
```

如果本月预算池耗尽：

- 抽卡仍可进行。
- 奖励可以变成星尘、称号、下月加成或象征性奖励。
- 不继续增加本月可消费金额。

---

## 9. 数据结构草案

以下为前端或轻量后端可使用的数据模型草案。

如果第一版只做本地应用，可以使用 localStorage / IndexedDB。
如果希望后续扩展，可以使用 SQLite / Supabase / Firebase。

---

## 9.1 UserSettings

用于保存系统设置。

```ts
type UserSettings = {
  id: string;
  monthlyBudgetLimit: number;      // 每月奖励预算上限，单位：元
  gachaCost: number;               // 单抽消耗宝石
  gachaOpenDays: number[];         // 卡池开放日，0=周日，6=周六
  enablePity: boolean;             // 是否启用保底
  timezone: string;                // 时区，例如 Asia/Tokyo
  createdAt: string;
  updatedAt: string;
};
```

示例：

```json
{
  "id": "settings_001",
  "monthlyBudgetLimit": 500,
  "gachaCost": 100,
  "gachaOpenDays": [0, 6],
  "enablePity": true,
  "timezone": "Asia/Tokyo",
  "createdAt": "2026-04-29T10:00:00+09:00",
  "updatedAt": "2026-04-29T10:00:00+09:00"
}
```

---

## 9.2 Task

用于保存任务基础信息。

```ts
type TaskType = "daily" | "series" | "main";
type TaskStatus = "active" | "completed" | "archived";
type TaskDifficulty = "easy" | "medium" | "hard" | "breakthrough";

type Task = {
  id: string;
  type: TaskType;
  title: string;
  description?: string;
  status: TaskStatus;
  difficulty?: TaskDifficulty;

  rewardGems: number;
  rewardDust: number;

  dueDate?: string;
  createdAt: string;
  updatedAt: string;
};
```

示例：

```json
{
  "id": "task_001",
  "type": "main",
  "title": "完成数学作业第一版",
  "description": "先完成，不追求完美。",
  "status": "active",
  "difficulty": "medium",
  "rewardGems": 60,
  "rewardDust": 5,
  "dueDate": "2026-05-03",
  "createdAt": "2026-04-29T10:00:00+09:00",
  "updatedAt": "2026-04-29T10:00:00+09:00"
}
```

---

## 9.3 DailyTaskTemplate

用于每日任务模板。每天根据模板生成实例。

```ts
type DailyTaskTemplate = {
  id: string;
  title: string;
  description?: string;
  rewardGems: number;
  rewardDust: number;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
};
```

示例：

```json
{
  "id": "daily_template_001",
  "title": "整理桌面 2 分钟",
  "description": "只需要开始，不需要整理完。",
  "rewardGems": 5,
  "rewardDust": 0,
  "isActive": true,
  "createdAt": "2026-04-29T10:00:00+09:00",
  "updatedAt": "2026-04-29T10:00:00+09:00"
}
```

---

## 9.4 TaskInstance

用于记录某一天的任务完成情况。

```ts
type TaskInstance = {
  id: string;
  taskId?: string;
  templateId?: string;
  date: string;
  title: string;
  type: TaskType;
  isCompleted: boolean;
  completedAt?: string;
  rewardGems: number;
  rewardDust: number;
};
```

说明：

- 每日任务通常由 templateId 生成。
- 系列任务和主线任务通常由 taskId 关联。
- 为了保留历史，即使原任务标题修改，实例中也保存当时的 title。

---

## 9.5 SeriesTaskProgress

用于记录系列任务的周期进度。

```ts
type SeriesTaskProgress = {
  id: string;
  taskId: string;
  periodType: "week" | "month";
  periodStart: string;
  periodEnd: string;
  targetCount: number;
  completedCount: number;
  bonusClaimed: boolean;
  bonusGems: number;
  bonusDust: number;
};
```

示例：

```json
{
  "id": "series_progress_001",
  "taskId": "task_run_001",
  "periodType": "week",
  "periodStart": "2026-04-27",
  "periodEnd": "2026-05-03",
  "targetCount": 3,
  "completedCount": 2,
  "bonusClaimed": false,
  "bonusGems": 50,
  "bonusDust": 5
}
```

---

## 9.6 Wallet

用于保存用户当前资源和奖励金额。

```ts
type Wallet = {
  id: string;
  gems: number;
  dust: number;
  rewardBalance: number;       // 当前可用快乐预算
  monthlyUnlockedAmount: number;
  monthlySpentAmount: number;
  month: string;               // 例如 2026-05
  updatedAt: string;
};
```

示例：

```json
{
  "id": "wallet_001",
  "gems": 380,
  "dust": 24,
  "rewardBalance": 85,
  "monthlyUnlockedAmount": 180,
  "monthlySpentAmount": 95,
  "month": "2026-05",
  "updatedAt": "2026-05-03T22:00:00+09:00"
}
```

---

## 9.7 ResourceTransaction

用于记录所有资源变化。

```ts
type ResourceTransactionType =
  | "task_reward"
  | "series_bonus"
  | "gacha_cost"
  | "gacha_reward"
  | "manual_adjustment";

type ResourceTransaction = {
  id: string;
  type: ResourceTransactionType;
  gemsDelta: number;
  dustDelta: number;
  rewardBalanceDelta: number;
  relatedTaskId?: string;
  relatedGachaPullId?: string;
  note?: string;
  createdAt: string;
};
```

示例：

```json
{
  "id": "txn_001",
  "type": "task_reward",
  "gemsDelta": 60,
  "dustDelta": 5,
  "rewardBalanceDelta": 0,
  "relatedTaskId": "task_001",
  "note": "完成主线任务：完成数学作业第一版",
  "createdAt": "2026-05-01T20:00:00+09:00"
}
```

---

## 9.8 GachaPool

用于定义卡池。

```ts
type GachaPool = {
  id: string;
  name: string;
  description?: string;
  isActive: boolean;
  startAt?: string;
  endAt?: string;
  costGems: number;
  createdAt: string;
  updatedAt: string;
};
```

示例：

```json
{
  "id": "pool_weekend_001",
  "name": "周末奖励卡池",
  "description": "完成一周现实任务后开放的奖励卡池。",
  "isActive": true,
  "startAt": "2026-05-02T00:00:00+09:00",
  "endAt": "2026-05-03T23:59:59+09:00",
  "costGems": 100,
  "createdAt": "2026-04-29T10:00:00+09:00",
  "updatedAt": "2026-04-29T10:00:00+09:00"
}
```

---

## 9.9 GachaRewardTier

用于定义稀有度、概率和奖励金额。

```ts
type RewardRarity = "N" | "R" | "SR" | "SSR" | "UR";

type GachaRewardTier = {
  id: string;
  poolId: string;
  rarity: RewardRarity;
  probability: number;       // 0 到 1
  rewardAmount: number;      // 金额，单位：元
  displayName: string;
};
```

示例：

```json
[
  {
    "id": "tier_n",
    "poolId": "pool_weekend_001",
    "rarity": "N",
    "probability": 0.6,
    "rewardAmount": 5,
    "displayName": "普通奖励"
  },
  {
    "id": "tier_r",
    "poolId": "pool_weekend_001",
    "rarity": "R",
    "probability": 0.25,
    "rewardAmount": 15,
    "displayName": "稀有奖励"
  },
  {
    "id": "tier_sr",
    "poolId": "pool_weekend_001",
    "rarity": "SR",
    "probability": 0.1,
    "rewardAmount": 30,
    "displayName": "超稀有奖励"
  },
  {
    "id": "tier_ssr",
    "poolId": "pool_weekend_001",
    "rarity": "SSR",
    "probability": 0.04,
    "rewardAmount": 80,
    "displayName": "极稀有奖励"
  },
  {
    "id": "tier_ur",
    "poolId": "pool_weekend_001",
    "rarity": "UR",
    "probability": 0.01,
    "rewardAmount": 200,
    "displayName": "传说奖励"
  }
]
```

---

## 9.10 GachaPull

用于记录每次抽卡。

```ts
type GachaPull = {
  id: string;
  poolId: string;
  costGems: number;
  rarity: RewardRarity;
  rewardAmount: number;
  pityTriggered: boolean;
  createdAt: string;
};
```

示例：

```json
{
  "id": "pull_001",
  "poolId": "pool_weekend_001",
  "costGems": 100,
  "rarity": "SR",
  "rewardAmount": 30,
  "pityTriggered": false,
  "createdAt": "2026-05-03T20:00:00+09:00"
}
```

---

## 9.11 PityState

用于记录保底状态。

```ts
type PityState = {
  id: string;
  poolId: string;
  pullsSinceLastSR: number;
  pullsSinceLastSSR: number;
  updatedAt: string;
};
```

示例：

```json
{
  "id": "pity_001",
  "poolId": "pool_weekend_001",
  "pullsSinceLastSR": 6,
  "pullsSinceLastSSR": 24,
  "updatedAt": "2026-05-03T20:00:00+09:00"
}
```

---

## 9.12 SpendingRecord

用于记录快乐预算的消费。

```ts
type SpendingRecord = {
  id: string;
  amount: number;
  category?: string;
  note: string;
  spentAt: string;
  createdAt: string;
};
```

示例：

```json
{
  "id": "spend_001",
  "amount": 28,
  "category": "饮食",
  "note": "买咖啡",
  "spentAt": "2026-05-04T15:30:00+09:00",
  "createdAt": "2026-05-04T16:00:00+09:00"
}
```

---

## 10. 推荐技术方案

## 10.1 第一版技术栈

推荐：

- 前端框架：React 或 Next.js
- 样式：Tailwind CSS
- 动画：Framer Motion
- 图标：Lucide Icons
- 数据存储：localStorage / IndexedDB
- 部署：Vercel

### 为什么适合

- 开发速度快。
- 网页 UI 和动画足够支撑第一版仪式感。
- 可以很容易做成 PWA，添加到手机主屏幕。
- 不需要一开始处理复杂后端。
- 后续可以平滑迁移到 Supabase / SQLite。

## 10.2 数据存储建议

### 阶段 1：纯本地

适合 MVP。

可选：

- localStorage：最简单，但结构化和容量有限。
- IndexedDB：更适合稍复杂的数据。

建议：如果想快速开始，用 localStorage；如果想稍微规范一点，用 IndexedDB。

### 阶段 2：轻量后端

如果后续需要跨设备同步，可以接入：

- Supabase
- Firebase
- SQLite + 自己的 API

---

## 11. 抽卡动画 MVP 方案

### 11.1 第一版动画目标

第一版不追求游戏级动画，而是实现基本仪式感。

最低要求：

- 点击抽卡后有短暂停顿。
- 背景变暗。
- 出现光效或卡牌背面。
- 卡牌翻转。
- 根据稀有度展示不同强度的视觉反馈。
- 显示奖励结果。

### 11.2 可实现方案

使用 Web 技术：

- Framer Motion：卡牌翻转、淡入淡出、缩放。
- CSS 动画：光圈、闪烁、粒子感。
- Lottie：后续可以引入现成动画。
- 音效：点击、闪光、结果揭示。

### 11.3 MVP 动画流程

```text
点击抽卡
  ↓
按钮锁定
  ↓
资源扣除
  ↓
屏幕暗化
  ↓
卡牌背面出现
  ↓
光效增强
  ↓
卡牌翻转
  ↓
显示稀有度和金额
  ↓
点击确认后返回卡池页
```

---

## 12. 开发优先级

### P0：必须做

- 首页 / 今日面板
- 每日任务完成
- 系列任务完成
- 主线任务创建与完成
- 宝石资源累计
- 周末卡池开放判断
- 单抽功能
- 奖励进入钱包
- 消费记录
- 基础本地数据持久化

### P1：很重要，但可以第二轮做

- 保底机制
- 星尘兑换
- 抽卡历史
- 任务编辑
- 任务归档
- 每周统计
- 更好的抽卡动画
- PWA 支持

### P2：后续增强

- 月度总结
- 任务模板库
- 成就系统
- 称号系统
- 角色成长
- 更复杂卡池
- 多设备同步
- AI 任务建议

---

## 13. 第一阶段验收标准

MVP 完成后，至少需要满足以下条件：

1. 用户可以创建并完成任务。
2. 完成任务后资源正确增加。
3. 每日任务可以按日期刷新或区分。
4. 周末可以抽卡，平日不能抽卡。
5. 抽卡会扣除宝石并生成奖励。
6. 奖励金额会进入钱包余额。
7. 用户可以记录消费。
8. 刷新页面后数据不会丢失。
9. 用户可以连续使用一周完成一次完整循环。

---

## 14. 2–4 周验证计划

### 第 1 周：极简使用

目标：确认任务和资源机制是否跑得通。

观察：

- 是否每天打开？
- 每日任务是否太多？
- 奖励是否太少或太多？
- 主线任务是否愿意录入？

### 第 2 周：验证周末抽卡

目标：确认抽卡是否真的带来期待感。

观察：

- 平日是否会因为周末抽卡而更愿意做任务？
- 抽卡结果是否让人满意？
- 低稀有度是否会让人沮丧？
- 保底是否必要？

### 第 3 周：调整数值

目标：调整任务奖励、抽卡消耗和奖励金额。

观察：

- 一周能抽几次比较合适？
- 主线任务奖励是否足够强？
- 每月预算是否容易超出？

### 第 4 周：决定是否继续工程化

判断标准：

- 使用频率是否稳定？
- 现实任务完成率是否提高？
- 用户是否期待周末抽卡？
- 奖励钱包是否减少消费内疚？
- 是否愿意继续维护这个系统？

如果答案大多为是，就值得进入第二阶段开发。

---

## 15. 后续迭代方向

### 15.1 机制增强

- 星尘兑换商店
- 成就系统
- 称号系统
- 连续登录奖励
- 主线章节
- 长期目标树
- 惩罚轻量化机制

### 15.2 表现增强

- 更精致的卡牌 UI
- 稀有度专属动效
- 音效系统
- 周末卡池主题
- 奖励弹窗
- 月度总结海报

### 15.3 数据增强

- 每周完成率图表
- 习惯趋势
- 奖励与完成率关系
- 主线任务推进速度
- 数据导出

### 15.4 平台增强

- PWA
- 移动端适配
- 云同步
- 桌面端封装
- 原生 App

---

## 16. 最小开发路线图

### Milestone 1：任务和资源闭环

目标：完成任务后能获得资源。

包含：

- 首页
- 任务列表
- 完成任务
- 资源累计
- 本地保存

### Milestone 2：周末抽卡闭环

目标：资源可以用于抽卡，奖励进入钱包。

包含：

- 卡池页
- 开放时间判断
- 单抽
- 稀有度计算
- 奖励入账
- 抽卡记录

### Milestone 3：奖励钱包闭环

目标：奖励可以被记录和消费。

包含：

- 钱包余额
- 消费记录
- 月度预算
- 余额计算

### Milestone 4：体验增强

目标：让系统有基本仪式感。

包含：

- 抽卡动画
- 稀有度视觉效果
- 首页状态卡片
- 周末倒计时
- 完成任务反馈动画

---

## 17. 一句话总结

这个项目第一阶段不应该做成“游戏”，而应该做成一个有游戏化反馈的个人任务系统。MVP 的核心不是精美美术，而是验证：任务、延迟满足、周末抽卡和奖励预算这套循环，是否真的能让用户更愿意完成现实中的重要事情。

